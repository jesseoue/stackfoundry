{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "api-usage-dashboard",
  "type": "registry:item",
  "title": "API Usage Dashboard",
  "description": "Per-key usage cards, request totals, errors, and limit visibility.",
  "dependencies": [],
  "devDependencies": [],
  "registryDependencies": [
    "api-keys",
    "usage-metering"
  ],
  "files": [
    {
      "path": "apps/web/src/lib/api-usage-dashboard.ts",
      "type": "registry:file",
      "content": "export type ApiUsageDashboardItem = {\n  key: string;\n  title: string;\n  status?: \"active\" | \"draft\" | \"archived\";\n  metadata?: Record<string, unknown>;\n};\n\nexport function createApiUsageDashboardItem(item: ApiUsageDashboardItem) {\n  return {\n    key: item.key,\n    title: item.title,\n    status: item.status ?? \"active\",\n    metadata: item.metadata ?? {},\n  };\n}\n\nexport const apiUsageDashboardChecklist = [\n  \"Confirm ownership boundaries\",\n  \"Wire persistence or provider adapter\",\n  \"Add audit events for sensitive changes\",\n] as const;\n"
    },
    {
      "path": "apps/web/src/app/(console)/developers/usage/page.tsx",
      "type": "registry:page",
      "content": "const items = [\n  { title: \"Configure\", description: \"Set ownership, defaults, and permissions.\" },\n  { title: \"Operate\", description: \"Review status, recent activity, and unresolved work.\" },\n  { title: \"Verify\", description: \"Run the checklist before enabling this module in production.\" },\n];\n\nexport default function ApiUsageDashboardPage() {\n  return (\n    <main className=\"flex flex-col gap-6 p-6\">\n      <div>\n        <h1 className=\"text-2xl font-semibold\">API Usage Dashboard</h1>\n        <p className=\"text-muted-foreground\">Per-key usage cards, request totals, errors, and limit visibility.</p>\n      </div>\n      <div className=\"grid gap-3 md:grid-cols-3\">\n        {items.map((item) => (\n          <section key={item.title} className=\"rounded-lg border p-4\">\n            <h2 className=\"font-medium\">{item.title}</h2>\n            <p className=\"text-sm text-muted-foreground\">{item.description}</p>\n          </section>\n        ))}\n      </div>\n    </main>\n  );\n}\n"
    }
  ],
  "meta": {
    "category": "developer-platform",
    "env": [],
    "status": "experimental",
    "agents": {
      "skills": [
        "api-usage-dashboard"
      ],
      "rules": [
        "document-invariants",
        "verify-before-release",
        "keep-provider-boundary"
      ]
    }
  }
}
