{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm registry:build && pnpm --filter @stackfoundry/web build",
  "outputDirectory": "apps/web/.next"
}
