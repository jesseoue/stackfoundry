# Filter Panel Checklist

- [ ] module manifest validates
- [ ] install path is verified in a fresh app
- [ ] filters are validated server-side
- [ ] tenant and authorization scopes cannot be overridden
- [ ] filter state is URL-shareable
