# AI SDK Checklist

- [ ] module manifest validates
- [ ] model helper fails clearly when `AI_GATEWAY_MODEL` is absent
- [ ] gateway API key remains server-only
- [ ] streaming helper returns an AI SDK stream result
- [ ] docs reference model IDs and cost-sensitive behavior
