# Shared Technology Skills

Shared skills live in `registry/skills/<name>/SKILL.md` and can be referenced by module manifests with `agents.skills`.

Use shared skills for technology guidance that should travel with multiple modules, such as Drizzle, Stripe, or Next.js. Keep module-specific operating instructions in `registry/modules/<module>/skill/SKILL.md`.
